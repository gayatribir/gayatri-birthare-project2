import React from 'react'
import { AppContext } from '../context';

export default function Prompt(){
  // const appCtx = useContext(AppContext);

  return (
   
      <input role="textbox" type='text' className="" onChange=""></input>
 
  );
}